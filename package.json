{
  "name": "backend",
  "version": "1.0.0",
  "scripts": {
    "start": "uvicorn app:app --host 0.0.0.0 --port 8000 --reload",
    "test": "echo \"No test specified\" && exit 0"
  },
  "dependencies": {}
}
